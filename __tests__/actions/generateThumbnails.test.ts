import * as path from 'path';

import { generateThumbnail, generateThumbnails } from '../../src/actions/generateThumbnails';

describe('generateThumbnail', () => {
  test('should generate thumbnails', () => {
    const photoName = 'test.jpg';
    const photoPath = path.resolve(__dirname, '../fixtures/generateThumbnails/thumbnail-photo.jpg');

  });
});

describe('generateThumbnails', () => {
});

